name: kalkulator_bisnis_umkm
description: Aplikasi Kasir UMKM Sederhana
publish_to: 'none'
version: 1.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # --- PLUGIN WAJIB DARI KODINGANMU ---
  intl: ^0.19.0              # Untuk Format Rupiah
  shared_preferences: ^2.2.2 # Untuk Simpan Data
  share_plus: ^10.0.0         # Untuk Share Struk ke WA
  # ------------------------------------

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  # Aset gambar tidak saya masukkan karena kamu pakai Icon bawaan di main.dart
